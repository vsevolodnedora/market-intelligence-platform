# CLAUDE.md

Derived operational spec. This file is normative and compressed. It is not a prose summary.

Record format:
- `{id, source, scope, phase, kind, priority, text}`
- `kind ∈ {invariant, import_rule, build_order, acceptance, style_rule, out_of_scope}`
- Conflict resolution order for any ambiguity or tension: `correctness > clarity > maintainability > testability > consistency > concision`

## mission

- {id:"MIS-001",source:"idea §1 Objective",scope:"repo",phase:"all",kind:"invariant",priority:"correctness",text:"Build a Germany-retailable UCITS ETF execution, accounting, tax, and allocation-research kernel that is correct on cash-account admissibility, fees/settlement/accounting/tax state, broker replay/reconciliation, and economically falsifiable for low-turnover ETF trading."}
- {id:"MIS-002",source:"idea §1 What v1 is and is not",scope:"repo",phase:"v1",kind:"invariant",priority:"correctness",text:"Treat v1 as a narrowly scoped production-quality proof for one live ETF trading profile; do not treat it as a broad multi-venue, multi-session, multi-strategy platform or a scalable allocation engine."}
- {id:"MIS-003",source:"plan intro",scope:"repo",phase:"v1",kind:"invariant",priority:"maintainability",text:"Implement v1 as a modular monolith with synchronous in-process event delivery; preserve the ability to swap runtime/transport in v2 without changing domain contracts."}
- {id:"MIS-004",source:"idea §1 Primary success criteria",scope:"repo",phase:"acceptance",kind:"acceptance",priority:"correctness",text:"Treat the four acceptance gates as sequential, not parallel: Gate 1 operational correctness → Gate 2 cost-model closure → Gate 3 strategy-level alpha evidence → Gate 4 capital sufficiency."}
- {id:"MIS-005",source:"idea §1 Narrow-success state; plan §3.2 C14",scope:"repo",phase:"terminal-state",kind:"acceptance",priority:"correctness",text:"Recognize two valid v1 terminal states only: NARROW_SUCCESS and FULL_SUCCESS. NARROW_SUCCESS requires Gate 1 definitive and Gates 2–4 unasserted or deferred; FULL_SUCCESS requires Gate 1 definitive and Gates 2–4 pass."}
- {id:"MIS-006",source:"idea §4 Rule 2; code_style Rule priority",scope:"repo",phase:"all",kind:"style_rule",priority:"correctness",text:"Optimize for correctness first, economics second, breadth last. When rules conflict, resolve by correctness, then clarity, maintainability, testability, consistency, then concision."}

## repo topology

- {id:"TOP-001",source:"user repo note",scope:"docs",phase:"all",kind:"invariant",priority:"clarity",text:"Treat `docs/00-charter/idea_11.md` and `docs/00-charter/idea_sidecar.yaml` as the canonical charter pair."}
- {id:"TOP-002",source:"user repo note",scope:"docs",phase:"all",kind:"invariant",priority:"clarity",text:"Treat `docs/10-implementation/staged_plan_11_1.md`, `docs/10-implementation/plan_sidecar.yaml`, and `docs/10-implementation/code_style.md` as the canonical implementation-planning set."}
- {id:"TOP-003",source:"user repo note",scope:"docs/20-working",phase:"all",kind:"style_rule",priority:"maintainability",text:"Use `docs/20-working/` only for working-state artifacts such as `current_slice.md`, `assumptions_register.md`, `decision_log.md`, and `open_questions.md`; do not treat it as canonical design authority."}
- {id:"TOP-004",source:"plan §1 Layout",scope:"modules",phase:"implementation",kind:"build_order",priority:"maintainability",text:"Target module topology is `modules/{kernel,events,platform_runtime,market_policy,execution,portfolio_accounting,admission_control,tax,ops_reconciliation,strategy_simulation}` with one public `contract.py` per module."}
- {id:"TOP-005",source:"plan §1 Contract file rule",scope:"modules/*",phase:"all",kind:"import_rule",priority:"maintainability",text:"`contract.py` is the only public import surface of a module. Everything else—aggregates, adapters, projections, stores, composition roots—is internal and changeable without cross-module coordination."}
- {id:"TOP-006",source:"plan §1bis events",scope:"modules/events",phase:"step1+",kind:"invariant",priority:"maintainability",text:"`modules/events` is a shared zero-logic payload module for cross-module dataclasses/enums only; it exists to break contract cycles and must remain business-logic free."}
- {id:"TOP-007",source:"plan §2 Kernel",scope:"modules/kernel",phase:"step1+",kind:"invariant",priority:"correctness",text:"`modules/kernel` is thin and frozen early; it contains shared types, IDs, protocols, errors, and enums, and must contain zero business logic and no broker/tax/fee/settlement policy logic."}

## non-negotiable invariants

- {id:"INV-001",source:"idea §4 Rule 1; plan §3.5",scope:"repo",phase:"all",kind:"invariant",priority:"correctness",text:"Reject unsupported cases; if the system cannot prove cash, fee, session, eligibility, tax, or any required predicate input, do not admit the trade and do not approximate missing data."}
- {id:"INV-002",source:"idea §4 Rule 4; plan §3.4",scope:"economic-state",phase:"all",kind:"invariant",priority:"correctness",text:"The append-only journal owned by `portfolio_accounting` is the sole economic source of truth. Execution facts explain causality but do not compete with journal truth."}
- {id:"INV-003",source:"idea §4 Rule 3; idea §2",scope:"research-vs-live",phase:"all",kind:"invariant",priority:"correctness",text:"Keep research scope, live universe, and supported live execution profile explicitly separate. Research may exceed live scope; live scope must remain pre-approved and narrower."}
- {id:"INV-004",source:"idea §4 Rule 5; plan §3.6",scope:"tax,compliance",phase:"v1",kind:"invariant",priority:"correctness",text:"Human review is mandatory for ambiguous tax/compliance changes. Engine-authored proposals do not post authoritative tax/compliance state without reviewer-authorized record commands and evidence pointers."}
- {id:"INV-005",source:"idea §4 Rule 6; plan §3.7",scope:"reconciliation",phase:"live",kind:"invariant",priority:"correctness",text:"Operate every reconciliation channel in explicit `GREEN|AMBER|RED` state. `AMBER` is degraded-but-safe and uses conservative inputs; `RED` freezes the affected admission path until explicitly closed."}
- {id:"INV-006",source:"plan §3.7",scope:"reconciliation",phase:"live",kind:"invariant",priority:"correctness",text:"`RED` on `BROKER_CASH` freezes all buy admissions; `RED` on `COMPLIANCE` freezes affected tradable lines; `RED` on other channels freezes only paths that depend on that channel."}
- {id:"INV-007",source:"idea §5 production profile; plan §3.2",scope:"live-profile",phase:"v1 live",kind:"invariant",priority:"correctness",text:"Support exactly one live-admissible production execution profile at a time. Shadow profiles may exist in paper/simulation or as reference data, but may not become live-admissible before FULL_SUCCESS or explicit re-pinning plus fresh gate closure."}
- {id:"INV-008",source:"idea §5 production profile",scope:"live-profile",phase:"v1 live",kind:"invariant",priority:"correctness",text:"The supported v1 live profile is IBKR cash account, long-only UCITS ETFs, EUR-funded account, EUR trading lines only, continuous trading only, 09:00–17:30 CET only, `DAY_LIMIT` only, and one pinned route with documented rationale."}
- {id:"INV-009",source:"idea §5 production profile",scope:"execution-style",phase:"v1 live",kind:"out_of_scope",priority:"correctness",text:"Normal live operation excludes extended retail sessions, auction-phase trading, market orders, MOC/MOO/IOC, overnight persistence, and modify/replace as standard behavior."}
- {id:"INV-010",source:"idea §3 broker assumptions; plan §3.4; plan §3.5",scope:"cash-admission",phase:"live",kind:"invariant",priority:"correctness",text:"Treat broker-reported settled cash as authoritative for live admission. Local settlement forecast is advisory only and must never override broker-authoritative settled cash."}
- {id:"INV-011",source:"idea §3 settlement assumptions; plan §3.4; plan §3.2",scope:"settlement",phase:"all",kind:"invariant",priority:"correctness",text:"Model settlement as effective-dated regime reference data, not as a timeless constant. v1 may operate with one active regime only; in-flight regime transitions are unsupported and are reconciliation breaks."}
- {id:"INV-012",source:"plan §3.1; plan §3.4; plan §3.2",scope:"persistence",phase:"all",kind:"invariant",priority:"correctness",text:"Event store, journal, and effective-dated policy/reference rows are append-only. Do not update/delete prior facts; express corrections as new events or adjusting entries that reference originals."}
- {id:"INV-013",source:"plan §2.3",scope:"events",phase:"all",kind:"invariant",priority:"correctness",text:"Persist every event with monotonic `event_id`, fully qualified versioned `event_type`, `occurred_at`, `recorded_at`, `causation_id`, `correlation_id`, `schema_version`, and typed payload. Breaking payload changes require new event types, not mutation of old schemas."}
- {id:"INV-014",source:"plan §2.1-2.2",scope:"types",phase:"all",kind:"style_rule",priority:"correctness",text:"Use opaque strongly typed IDs at boundaries; never plain strings. Use `Decimal`-based `Money`; no floats in economic logic; cross-currency arithmetic requires explicit FX rate."}
- {id:"INV-015",source:"plan §2.4; code_style State and determinism",scope:"determinism",phase:"all",kind:"invariant",priority:"correctness",text:"Inject time, randomness, and external clients. Domain logic must not call `datetime.now()`, `uuid.uuid4()`, filesystem, network, or DB directly. `IdFactory` must support deterministic seeded replay mode."}
- {id:"INV-016",source:"idea §10; plan §3.1; plan §3.4",scope:"replay",phase:"all",kind:"invariant",priority:"correctness",text:"Replay determinism is mandatory: given the same event stream, reference snapshots, pinned parameters, canonical ordering key, and replay seed, derived economic state must be identical."}
- {id:"INV-017",source:"idea §3 broker reporting; plan §3.3",scope:"execution-identity",phase:"all",kind:"invariant",priority:"correctness",text:"`BrokerPermId` is the canonical API-originated identity. Fallback identity is allowed only for non-API activity when linked to governance exception or statement posting and marked `origin=non_api`."}
- {id:"INV-018",source:"idea §3 broker reporting; plan §3.3 C10",scope:"execution-corrections",phase:"all",kind:"invariant",priority:"correctness",text:"Treat an IBKR execution callback as a correction only when same `permId` + functionally identical execution parameters + different `execId`; corrections must share correction group and never mutate prior events."}
- {id:"INV-019",source:"idea §6; plan §3.5; plan §3.8",scope:"admission",phase:"v1 live",kind:"invariant",priority:"correctness",text:"Per-trade admission is fill-conditional in v1. `p_fill` is telemetry/stress only and must never be an input to the live admission scalar."}
- {id:"INV-020",source:"plan §3.5 C12",scope:"admission",phase:"step8+",kind:"invariant",priority:"correctness",text:"Compose live admissibility only in `admission_control.compose_live_admissibility(intrinsic, staleness, current_pinned_profile, terminal_state, as_of)`. `market_policy` returns intrinsic state only; `ops_reconciliation` returns staleness only."}
- {id:"INV-021",source:"plan §3.5; plan §9 self-check",scope:"admission",phase:"step8+",kind:"invariant",priority:"correctness",text:"`AdmissionInputs` must enumerate every predicate input. Missing required input must fail closed with `INCOMPLETE_INPUTS`; never silently default missing fields."}
- {id:"INV-022",source:"idea §6; plan §3.5",scope:"admission",phase:"step8+",kind:"invariant",priority:"correctness",text:"Admit a trade only if all predicates pass: live admissibility, fill-conditional excess benefit over full modeled hurdle, ticket size ≥ `N_min`, current tier permits post-trade position count, churn within cap, strategy certification valid, settled-cash sufficiency, re-submit budgets within caps, channels not `RED`, and liquidity/session constraints satisfied."}
- {id:"INV-023",source:"plan §3.5; plan §3.4",scope:"capital-policy",phase:"v1 live",kind:"invariant",priority:"correctness",text:"Do not compute `AUM_min` on the per-trade admission path. Per-trade admission reads current governance tier; `AumMinComputer` is used on Gate-4 certification/reporting paths only."}
- {id:"INV-024",source:"idea §6; plan §3.2; plan §3.5",scope:"governance-parameters",phase:"gates 2-3",kind:"acceptance",priority:"correctness",text:"Parameters tagged `PLACEHOLDER` may be used for research/simulator runs but may not support Gate-2 or Gate-3 verdicts or certifications."}
- {id:"INV-025",source:"idea §6 tier policy; plan §3.2",scope:"tiers",phase:"all",kind:"invariant",priority:"correctness",text:"Tier transitions are governance events, not automatic reactions to AUM. Admission reads tier; it does not compute or auto-promote tiers."}
- {id:"INV-026",source:"idea §12-13; plan §3.3 C3; plan §3.8",scope:"limit-price",phase:"step3+",kind:"invariant",priority:"correctness",text:"Deterministic limit-price construction is owned by `execution.SubmitOrder` via shared pure `LimitPriceComputer`; simulator must call the identical function so live and simulated limit-price behavior cannot drift."}
- {id:"INV-027",source:"idea §12; plan §3.4 C4; plan §3.2",scope:"fees",phase:"all",kind:"invariant",priority:"correctness",text:"Model, post, reconcile, and test fee components at component granularity: commission, exchange, clearing, regulatory, and transaction tax each have distinct posting account codes and distinct closure criteria."}
- {id:"INV-028",source:"idea §9-10; plan §3.6; plan §3.4",scope:"tax",phase:"all",kind:"invariant",priority:"correctness",text:"Keep statutory tax ledger and tax-cash ledger structurally distinct. Only cashflow-bearing tax events cross into the journal; statutory state updates alone do not post cash journal entries."}
- {id:"INV-029",source:"idea §9; plan §3.6",scope:"tax",phase:"all",kind:"invariant",priority:"correctness",text:"Do not silently absorb annual tax residuals. Residuals above tolerance open explicit tax exceptions and stay open until closed by correction, bridge adjustment, or signed de-minimis decision."}
- {id:"INV-030",source:"idea §17 scope-expansion; plan §3.2 C13/C14",scope:"profiles,gates",phase:"all",kind:"acceptance",priority:"correctness",text:"Re-pinning a production profile immediately invalidates prior Gate-2 support tied to the old profile and immediately invalidates strategy certifications bound to the old profile; re-certification requires a fresh canonical run plus fresh mandatory stress overlay coverage under the new profile."}
- {id:"INV-031",source:"idea §17 scope-expansion; plan §3.2",scope:"profiles",phase:"v1",kind:"acceptance",priority:"correctness",text:"Do not add a second live-admissible route/session/profile before prior FULL_SUCCESS. Promotion of a shadow profile requires governance re-pinning plus fresh Gate 1 and Gate 2 closure for that profile."}
- {id:"INV-032",source:"idea §16; plan §3.2 C14; plan §6 step0",scope:"phase0",phase:"step0",kind:"acceptance",priority:"correctness",text:"Phase 0 pre-screen runs before Phase-A engineering. `PASS` unblocks step 1; `FAIL` blocks step 1 until a fresh protocol passes or a signed re-scope decision is recorded. Phase-0 fail is not a v1 terminal state."}
- {id:"INV-033",source:"idea §16; plan §3.2 C14; plan §3.8",scope:"phase0",phase:"step0",kind:"invariant",priority:"correctness",text:"Freeze the four Phase-0 items—research protocol, OOS window, required effect size, pre-registered decision rule—before any OOS numerics are computed. Re-running against the same research configuration with relaxed thresholds is refused."}
- {id:"INV-034",source:"idea §17; plan §3.2 C14",scope:"terminal-state",phase:"acceptance",kind:"acceptance",priority:"correctness",text:"`NARROW_SUCCESS` is admissible only when Gate 1 is `DEFINITIVE` and Gates 2–4 are `UNASSERTED` or `DEFERRED`; `FAIL` or `INVALIDATED` on Gates 2–4 blocks narrow-success declaration until remediated or formally deferred."}
- {id:"INV-035",source:"idea §17; plan §3.2 C14",scope:"terminal-state",phase:"acceptance",kind:"acceptance",priority:"correctness",text:"`FULL_SUCCESS` is admissible only when Gate 1 is `DEFINITIVE` and Gates 2–4 are `PASS`. `PROVISIONAL` Gate 1 is insufficient for terminal-state declaration."}
- {id:"INV-036",source:"idea §18",scope:"v2-precondition",phase:"roadmap",kind:"acceptance",priority:"correctness",text:"Do not treat v2 work as unlocked from narrow-success. v2 requires full-success v1; continuation work that closes Gates 2–4 remains v1 work, not v2 work."}

## allowed import graph

- {id:"IMP-001",source:"plan §5 rule 1",scope:"modules/kernel",phase:"all",kind:"import_rule",priority:"maintainability",text:"`kernel` imports nothing from this repo."}
- {id:"IMP-002",source:"plan §5 rule 2",scope:"modules/events",phase:"all",kind:"import_rule",priority:"maintainability",text:"`events` imports `kernel` only and contains zero business logic, zero `Protocol`s, and zero query types."}
- {id:"IMP-003",source:"plan §5 rules 3-4",scope:"modules/*",phase:"all",kind:"import_rule",priority:"maintainability",text:"Every other module may import `kernel`, `events`, and another module's `contract.py` only; never import another module's internals."}
- {id:"IMP-004",source:"plan §5 rule 2",scope:"events.contract",phase:"all",kind:"import_rule",priority:"maintainability",text:"A payload type belongs in `events.contract` iff putting it in a domain contract would create a contract cycle or the payload is consumed unchanged by three or more domain modules."}
- {id:"IMP-005",source:"plan §5 rule 3; plan §3.1 M1",scope:"modules/platform_runtime",phase:"step2+",kind:"import_rule",priority:"maintainability",text:"`platform_runtime` must not statically import domain contracts for canonical ordering. It takes a `Callable[[Event], tuple]` ordering key injected at composition time."}
- {id:"IMP-006",source:"plan §5 dependency graph",scope:"modules",phase:"all",kind:"import_rule",priority:"maintainability",text:"Compile-time dependency direction is: `kernel <- events <- platform_runtime <- market_policy <- execution <- portfolio_accounting <- tax <- ops_reconciliation <- admission_control <- strategy_simulation`. Preserve acyclicity."}
- {id:"IMP-007",source:"plan §5 rules 5-6",scope:"modules",phase:"all",kind:"import_rule",priority:"maintainability",text:"Do not use `TYPE_CHECKING` to paper over contract cycles. Former cycle-breaking payloads must remain in `events.contract`, and live-admissibility composition must remain in `admission_control.contract`."}
- {id:"IMP-008",source:"plan §5 rule 7",scope:"tests",phase:"all",kind:"import_rule",priority:"testability",text:"Tests may import module internals only from that module's own test suite."}
- {id:"IMP-009",source:"plan §5 rules 8-10; code_style Dependency injection",scope:"adapters",phase:"all",kind:"import_rule",priority:"testability",text:"External adapters live in their owning modules and are injected via protocols/ABCs. `contract.py` remains type-only plus pure side-effect-free helper functions."}

## implementation order

- {id:"BLD-001",source:"plan §6 step0",scope:"repo",phase:"step0",kind:"build_order",priority:"correctness",text:"Build Phase-0 subset first: minimal `kernel`, minimal `events`, minimal `market_policy`, and Phase-0-only `strategy_simulation` sufficient to register protocol, run pre-screen, and record Phase-0 verdict."}
- {id:"BLD-002",source:"plan §6 step0",scope:"repo",phase:"step0-exit",kind:"build_order",priority:"correctness",text:"Do not proceed to step 1 until Phase-0 verdict is `PASS` or a signed re-scope decision explicitly permits it."}
- {id:"BLD-003",source:"plan §6 step1",scope:"modules/kernel,modules/events",phase:"step1",kind:"build_order",priority:"maintainability",text:"Freeze full `kernel` and full `events` surfaces at step 1. Later additive changes require coordinated versioning across downstream consumers."}
- {id:"BLD-004",source:"plan §6 step2",scope:"modules/platform_runtime",phase:"step2",kind:"build_order",priority:"correctness",text:"Build runtime next: append-only event store, in-process bus, outbox, replay runner, projections, and seeded replay-mode `IdFactory`. Use injected ordering key, not static execution imports."}
- {id:"BLD-005",source:"plan §6 step3",scope:"modules/execution",phase:"step3",kind:"build_order",priority:"correctness",text:"Then build minimum `execution`: broker ingest, canonical identity, correction grouping, fallback identity, re-submit budgets, market snapshots, limit-price construction, correction-detection rule, and runtime configuration tagging. Do not submit live orders yet."}
- {id:"BLD-006",source:"plan §6 step4",scope:"modules/portfolio_accounting",phase:"step4",kind:"build_order",priority:"correctness",text:"Then build minimum `portfolio_accounting`: append-only journal, cash decomposition, reserved cash, correction-safe posting, churn budget, `AumMinComputer`, and fee-component postings. Demonstrate deterministic replay end-to-end on synthetic streams here."}
- {id:"BLD-007",source:"plan §6 step5",scope:"modules/market_policy",phase:"step5",kind:"build_order",priority:"correctness",text:"Then build full `market_policy`: effective-dated reference data, evidence registry, tradable-line approval, settlement regimes, parameter pinning, gate verdicts, fee schedules, liquidity caps, terminal-state declaration, profile pinning/re-pinning, and tier management."}
- {id:"BLD-008",source:"plan §6 step6",scope:"modules/tax",phase:"step6",kind:"build_order",priority:"correctness",text:"Then build `tax`: two-ledger shape, bridge/exception workflow, reviewer-authorized records, auto-exception on prior-year corrections, annual posting planner, and per-trade tax-friction reader."}
- {id:"BLD-009",source:"plan §6 step7",scope:"modules/ops_reconciliation",phase:"step7",kind:"build_order",priority:"correctness",text:"Then build `ops_reconciliation`: six-channel Green/Amber/Red state, freeze signals, conservative reads, fallback monitoring, re-submit-window break handling, and compliance staleness scanning/reader."}
- {id:"BLD-010",source:"plan §6 step8",scope:"modules/admission_control",phase:"step8",kind:"build_order",priority:"correctness",text:"Then build `admission_control`: materialize complete `AdmissionInputs`, compute composed live admissibility, export pure `evaluate`, and enforce broker-cash-authoritative live admission with reason-code-rich verdicts."}
- {id:"BLD-011",source:"plan §6 step9",scope:"modules/strategy_simulation",phase:"step9",kind:"build_order",priority:"correctness",text:"Build full `strategy_simulation` last: canonical simulator, overlays, rebalance/intents, Gate-3 evidence, profile-bound canonical runs, and shared use of live admission and limit-price pure functions."}
- {id:"BLD-012",source:"plan §6 post-build",scope:"repo",phase:"paper/live rollout",kind:"build_order",priority:"correctness",text:"Begin paper trading after step 8 using `PAPER` runtime configuration. Begin Tier-0 micro-live only after Gate-1 `PROVISIONAL` closes on synthetic+paper streams. Begin Gate-2 evaluation only after Gate-1 `DEFINITIVE` closes on Tier-0 micro-live."}

## testing / ci definition of done

- {id:"ACC-001",source:"code_style Repository baseline; plan §8 intro",scope:"repo",phase:"every change",kind:"acceptance",priority:"testability",text:"Repo baseline is mandatory: Ruff, Pyright strict mode, Pytest, and CI running lint, types, and tests on every change. Code is incomplete unless all pass."}
- {id:"ACC-002",source:"code_style Definition of done; plan §8 intro",scope:"repo",phase:"every change",kind:"acceptance",priority:"testability",text:"A module/change is complete only when its contract compiles, type-checks, required tests exist and pass, import-linter/tach rules pass, and behavior/logging/errors are deterministic and maintainable."}
- {id:"ACC-003",source:"plan §8.1-8.10",scope:"repo",phase:"every module",kind:"acceptance",priority:"testability",text:"Each module must ship explicit automated tests for its stated invariants; CI is not a syntax-only gate. Property/integration/end-to-end tests are required where the plan assigns them."}
- {id:"ACC-004",source:"plan §8.1bis; plan §5",scope:"modules/events,modules/*",phase:"ci",kind:"acceptance",priority:"maintainability",text:"CI must enforce acyclic imports, `events.contract` purity, and contract-only cross-module imports. A new import cycle fails the build."}
- {id:"ACC-005",source:"plan §8.2-8.4",scope:"runtime,execution,accounting",phase:"ci",kind:"acceptance",priority:"correctness",text:"CI must prove append-only persistence, replay determinism under pinned seed, outbox atomicity, correction-safe execution identity handling, deterministic ordering, journal invariance, reserved-cash correctness, and settlement guardrails."}
- {id:"ACC-006",source:"plan §8.5-8.9",scope:"policy,admission,tax,reconciliation,simulation",phase:"ci",kind:"acceptance",priority:"correctness",text:"CI must prove additive effective-dated policy behavior, gate/profile invalidation behavior, predicate determinism, conservative reads, candidate/approval tax flow, channel-state determinism, simulator validity, profile binding, and Phase-0 structural constraints."}
- {id:"ACC-007",source:"plan §8.10",scope:"cross-module",phase:"ci",kind:"acceptance",priority:"correctness",text:"CI must include cross-module rehearsals for gate progression, route re-pinning, compliance staleness freezes, paper-to-live cutover, replay under broker corrections, and no-cycle enforcement."}
- {id:"ACC-008",source:"idea §17 Gate 1; plan §3.2 C8; plan §6",scope:"acceptance/gate1",phase:"gate1",kind:"acceptance",priority:"correctness",text:"Gate 1 passes in two stages: `PROVISIONAL` after synthetic+paper evidence proves deterministic replay and clean path behavior sufficient for Tier-0 micro-live; `DEFINITIVE` after Tier-0 micro-live reconciles cleanly. Gate 2 and terminal-state declaration require `DEFINITIVE`, not merely `PROVISIONAL`."}
- {id:"ACC-009",source:"idea §17 Gate 2",scope:"acceptance/gate2",phase:"gate2",kind:"acceptance",priority:"correctness",text:"Gate 2 requires pinned §6 governance parameters, live exemplars across every fee/slippage dimension the pinned profile actually touches, closure on the pinned route only, and zero forgiven residuals. Route comparison is not a Gate-2 input in v1."}
- {id:"ACC-010",source:"idea §17 Gate 3",scope:"acceptance/gate3",phase:"gate3",kind:"acceptance",priority:"correctness",text:"Gate 3 requires Gate 2 closed, out-of-sample strategy-level LCB evidence over the research universe, all §6 parameters pinned, and a pass under mandatory fill-probability stress overlay. Live rotation beyond Tier 0 is blocked without this certification."}
- {id:"ACC-011",source:"idea §17 Gate 4",scope:"acceptance/gate4",phase:"gate4",kind:"acceptance",priority:"correctness",text:"Gate 4 requires `AUM_min(k,r,t)` for the pinned route, cash buffer coverage for expected tax postings and fee-misclassification reserve, and observed live ticket sizes consistent with `N_min`. Tier raises depend on this gate."}
- {id:"ACC-012",source:"idea §17 scope-expansion; idea §18",scope:"acceptance/scope",phase:"post-full-success",kind:"acceptance",priority:"correctness",text:"Do not expand to a second live route/session or v2 work until all four gates pass for the first profile; any new profile must independently clear at least Gate 1 and Gate 2."}

## forbidden patterns

- {id:"FORB-001",source:"idea §2 Out of scope for v1",scope:"repo",phase:"v1",kind:"out_of_scope",priority:"correctness",text:"Do not implement multi-venue live routing, extended retail live trading, auction trading, market orders, intraday reactive trading, holdings-propagated issuer-event alpha, NLP/LLM-driven alpha, automated ambiguous tax/compliance sign-off, or more than one supported live execution profile."}
- {id:"FORB-002",source:"idea §2 deferred; idea §18",scope:"repo",phase:"v1",kind:"out_of_scope",priority:"correctness",text:"Do not build or gate v1 on endogenous route-comparison science, forward-dated settlement-regime cutover machinery, per-trade LCB gating, hard XLM-driven universe freeze, or a multi-mode simulator."}
- {id:"FORB-003",source:"code_style Forbidden shortcuts",scope:"code",phase:"all",kind:"style_rule",priority:"maintainability",text:"Do not introduce `Any` except at unavoidable third-party boundaries; do not return `dict[str, Any]` across business/module/service/persistence/API boundaries; do not use ambiguous return shapes or boolean flag bundles for state."}
- {id:"FORB-004",source:"code_style Validation, Function design, Dependency injection",scope:"code",phase:"all",kind:"style_rule",priority:"correctness",text:"Do not mix parsing, validation, I/O, and business rules in one function. Validate once at the boundary, convert to typed internal structures, and keep pure logic separate from orchestration and adapters."}
- {id:"FORB-005",source:"code_style Configuration",scope:"config",phase:"all",kind:"style_rule",priority:"maintainability",text:"Do not read env vars throughout the codebase and do not pass raw config dicts through business logic. Load and validate config once at startup into typed objects."}
- {id:"FORB-006",source:"code_style Errors and exceptions",scope:"errors",phase:"all",kind:"style_rule",priority:"correctness",text:"Do not catch broad exceptions without meaningful handling, do not swallow errors, and do not collapse everything into generic application errors unless the external interface requires it."}
- {id:"FORB-007",source:"code_style Testing",scope:"tests",phase:"all",kind:"style_rule",priority:"testability",text:"Do not write code that requires patching internals to test. Prefer injected fakes/test doubles, and keep unit tests free of real network, time, randomness, and shared mutable state."}
- {id:"FORB-008",source:"plan §5 rules 4-5",scope:"modules",phase:"all",kind:"import_rule",priority:"maintainability",text:"Do not import module internals across module boundaries, and do not reintroduce contract cycles or `TYPE_CHECKING`-based cycle workarounds."}
- {id:"FORB-009",source:"plan §3.3; idea §3 broker assumptions",scope:"execution",phase:"v1 live",kind:"out_of_scope",priority:"correctness",text:"Do not treat modify/replace or overnight persistence as normal live paths, and do not rely on directed Tiered pricing for directed API stock orders in v1."}
- {id:"FORB-010",source:"plan §3.8",scope:"strategy_simulation",phase:"all",kind:"out_of_scope",priority:"correctness",text:"`strategy_simulation` must never submit orders directly. It emits intents/proposals/evidence only."}
- {id:"FORB-011",source:"plan §3.7",scope:"ops_reconciliation",phase:"all",kind:"out_of_scope",priority:"correctness",text:"`ops_reconciliation` must never write to the journal or execution-event ledger; it surfaces channel state, conservative inputs, and breaks only."}
- {id:"FORB-012",source:"plan §3.2 C12",scope:"market_policy",phase:"all",kind:"import_rule",priority:"maintainability",text:"Do not embed composed `is_live_admissible` logic in `market_policy`; doing so would violate the dependency graph and recreate an import cycle with `ops_reconciliation`."}

## when uncertain, fail closed

- {id:"FAIL-001",source:"code_style Errors and exceptions; idea §4 Rule 1",scope:"repo",phase:"all",kind:"style_rule",priority:"correctness",text:"Do not guess in ambiguous situations. Preserve context, return explicit structured errors/verdicts, and prefer rejection or blocked progression over optimistic defaulting."}
- {id:"FAIL-002",source:"plan §3.5",scope:"admission",phase:"live",kind:"invariant",priority:"correctness",text:"If any required admission input is absent, incomplete, or stale beyond allowed state, reject with explicit reason codes rather than synthesizing or defaulting inputs."}
- {id:"FAIL-003",source:"plan §3.7; idea §4 Rule 6",scope:"reconciliation",phase:"live",kind:"invariant",priority:"correctness",text:"If a channel reaches `RED`, freeze the affected admission path immediately and require explicit break closure with recorded resolution before resuming."}
- {id:"FAIL-004",source:"plan §3.2; plan §6 step0",scope:"phase0",phase:"pre-step1",kind:"acceptance",priority:"correctness",text:"If Phase 0 verdict is `FAIL`, block Phase-A engineering. Only a fresh protocol pass or signed re-scope decision can reopen progression."}
- {id:"FAIL-005",source:"plan §3.2; idea §17",scope:"gates",phase:"gates2-4",kind:"acceptance",priority:"correctness",text:"If any earlier gate is not closed, do not claim a later gate. If a required verdict is `FAIL` or `INVALIDATED`, do not declare narrow/full success until remediated or formally deferred where allowed."}
- {id:"FAIL-006",source:"plan §3.2; idea §17 scope-expansion",scope:"profiles",phase:"v1",kind:"acceptance",priority:"correctness",text:"If a requested change would create a second live-admissible profile/session/route before FULL_SUCCESS, refuse it."}
- {id:"FAIL-007",source:"plan §3.2; code_style Public interfaces",scope:"schemas,contracts",phase:"all",kind:"style_rule",priority:"maintainability",text:"If a contract, event, CLI, or persisted schema change would be breaking, make the break explicit via versioned boundaries and corresponding tests; do not ship silent boundary breaks."}
- {id:"FAIL-008",source:"code_style Default expectation",scope:"repo",phase:"all",kind:"style_rule",priority:"clarity",text:"If an implementation choice would leave a new engineer guessing, reject that choice and prefer the more explicit, typed, deterministic, and testable design."}
